"""This is the main entry point for the EntityNetwork package."""
from entitynetwork import cli


def main():
    """Starts the command line interface."""
    cli.start()


if __name__ == "__main__":
    """Main entry point if module is called directly."""
    cli.start()
